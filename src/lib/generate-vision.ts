/** Max listing photos sent to OpenAI vision for caption generation. */
export const MAX_VISION_IMAGES = 3;

/** Max edge for API vision images — low detail does not need full upload resolution. */
export const API_VISION_IMAGE_MAX_EDGE = 768;
